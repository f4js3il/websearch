spring:
#  main:
#    web-application-type: reactive
  webflux:
    request-timeout: 120s
  application:
    name: websearch
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          max-tokens: 500
          tool-names: web_search
server:
  port: 10002
